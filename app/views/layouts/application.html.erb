<!DOCTYPE html>
<html>
  <head>
    <title>SambaFm</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header class="navbar navbar-fixed-top">
    <div class=navbar-inner">
      <div class="container">
        <nav>
        <ul class="nav pull-right">
          <li><%= link_to "Home", root_path, id: "logo" %></li>
          <li><%= link_to "Blogs", blogs_path %></li>
          <% if user_signed_in? %>
            <li><%= link_to "Library", library_user_path(current_user), id: "library_link" %></li>
          <% end %>
        </ul>
        </nav>
      </div>
    </div>
    </header>
    <div class="container">
      <section class="round">
      <% flash.each do |k, v| %>
        <div class="flash <%= k %>"><%= v %></div>
      <% end %>
      <%= yield %>
      </section>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
